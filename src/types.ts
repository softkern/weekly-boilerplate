export interface MetaData {
  title: string
  description?: string
  image?: string
  article?: boolean
  publishDate?: Date
}

export interface Site {
  url: string
  title: string
  description?: string
}
